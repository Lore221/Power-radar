package com.limbo2136.powerradar.radar.network;

/** Имена ролей сохраняются в NBT Radar Link; существующие значения нельзя переименовывать. */
public enum RadarLinkEndpointRole {
    NONE,
    RADAR_CONTROLLER,
    RADAR_MONITOR,
    COMPUTING_BLOCK,
    FUTURE_CONSUMER
}
